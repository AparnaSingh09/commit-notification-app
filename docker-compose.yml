# Explicit project name so this stays a fully separate Docker project from
# docker-compose.full.yml - without this, both files default to the same
# project name (the directory name) and end up sharing one network/orphan
# list, which defeats the point of keeping them independent.
name: commit-notification-app-dev

services:
  mongo:
    image: mongo:7
    container_name: commit-notif-mongo
    command: ["--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    healthcheck:
      test: >
        mongosh --quiet --eval "db.adminCommand('ping')"
      interval: 5s
      timeout: 5s
      retries: 10

  mongo-init:
    image: mongo:7
    container_name: commit-notif-mongo-init
    depends_on:
      mongo:
        condition: service_healthy
    restart: "no"
    entrypoint: >
      mongosh --host mongo --quiet --eval "
        try {
          rs.status();
          print('replica set already initiated');
        } catch (e) {
          rs.initiate({_id: 'rs0', members: [{_id: 0, host: 'localhost:27017'}]});
          print('replica set initiated');
        }
      "
    # NOTE: host is 'localhost:27017' (not 'mongo:27017') because the Go backend
    # currently runs on the host machine, not in this compose network. When we
    # dockerize the backend later, this needs to change back to 'mongo:27017'
    # (or the containers need a shared network alias) - revisit at that step.

volumes:
  mongo-data:
    # Pinned to the exact name Compose already created before this file had
    # an explicit project name, so existing data keeps being reused instead
    # of the new project name silently starting a fresh empty volume.
    name: commit-notification-app_mongo-data
