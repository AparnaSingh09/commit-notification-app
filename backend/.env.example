PORT=8080

MONGO_URI=mongodb://localhost:27017/?replicaSet=rs0
MONGO_DB=commit_notification_app

JWT_SECRET=change-me

BITBUCKET_CLIENT_ID=
BITBUCKET_CLIENT_SECRET=
BITBUCKET_CALLBACK_URL=http://localhost:8080/auth/callback/

ANTHROPIC_API_KEY=
GROQ_API_KEY=

FRONTEND_URL=http://localhost:5173

# Non-secret application settings (which LLM provider/model, safety limits,
# poll/summary intervals, etc.) live in config.json, not here - it's safe
# to commit (no secrets in it) and easier to review than env vars. See
# config.json in this directory.
